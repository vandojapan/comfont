# AviUtl2 合成フォントプラグイン — 開発コンテキスト（Claude→Codex 引き継ぎ）

## プロジェクト概要

AviUtl2向けに、Illustratorの「合成フォント」機能（文字種ごとに異なるフォントを自動適用する仕組み）を再現するプラグインをRustで開発する。主軸言語はRust、SDKバインディングは[aviutl2-rs](https://github.com/sevenc-nanashi/aviutl2-rs)（crates.io: `aviutl2`、開発時点で v0.40.0〜0.41.0）を使用。

## 想定用途（最重要の制約）

**PSDToolKitの字幕表示**が主なユースケース。ユーザーはすでに、セリフ準備オブジェクトから`Lua`グローバル経由でキャラクターIDを読み取り、フレーム間で持続させながら、ダブルレイヤーのテキスト制御スクリプトで文字ごとのフォント切り替えを手組みで実装した実績がある。今回のプラグインは、この**手組みロジックを置き換える／肩代わりするもの**であり、以下が絶対条件：

- テキストオブジェクトは**ネイティブのまま**であること（字間・行間など既存のオブジェクトパラメータがそのまま機能する必要がある）
- 制御スクリプト（PSDToolKit側のスクリプト資産を含む）から呼び出せる形であること

## 確定した設計

### 1. 合成フォントエディタ（メイン・`generic`プラグイン）

- 文字種／Unicode範囲（かな・カナ・漢字・欧文・数字・記号など）ごとに「フォントファミリー・サイズ比率・ベースラインシフト・字送り補正」を対応付けるルールセットをGUIで編集
- プロファイルとして永続化（`serde` featureで`config`モジュール経由の保存を想定）
- 本文テキストや字間・行間はここには含めない（Illustratorの合成フォントも「フォント資産」であり文章そのものは持たないのと同じ思想）

### 2. 合成フォント処理モジュール（サブ・`module` = スクリプトモジュールプラグイン）

- `ScriptModule`トレイトを実装し、`#[aviutl2::plugin(ScriptModule)]` + `functions`属性マクロ + `register_script_module!`で登録
- 制御スクリプトから呼べる関数（例: `compositefont.resolve(char, profile)`）を提供し、1文字ごとに「どのカテゴリに属するか判定→対応フォント・サイズ比・ベースラインシフトを返す」処理をRust側で肩代わりする
- Lua側は返り値を使って`obj.setfont`やオフセット補正を適用するだけの薄い実装に留める
- 参考実装例: `examples/username-module`（リポジトリ内）

## 検討して却下した方式（経緯・理由）

| 方式 | 却下理由 |
|---|---|
| 合成OTFを生成してフォントローダーに読み込ませる | 複数フォントのグリフを1つの新規フォントバイナリにコピー・合成することになり、フォントEULAの改変・再配布禁止条項に抵触するリスクが高い |
| `input`プラグインとして構成ファイルをラスタライズ画像として読み込ませる | テキストが静的画像化されてしまい、字間・行間などオブジェクト側の調整が効かなくなる |
| `filter`プラグイン（カスタムオブジェクト、独自にトラックバー付き生成描画） | 字間・行間はトラックバーとして持たせられるが、PSDToolKitの制御スクリプトが読み書きする対象がネイティブのテキストオブジェクトではなくなり、既存のスクリプト資産と噛み合わなくなる |

いずれも、最終的に「ネイティブテキストオブジェクト＋制御スクリプトからのモジュール呼び出し」という現行方式に着地した決め手は、PSDToolKit字幕運用との整合性。

## aviutl2-rsクレートに関する技術メモ

- クレート構成: `input` / `output` / `filter` / `module` / `generic` / `logger` / `config` / `cache` / `common` / `utils`（各機能はfeature flagで個別に有効化可能、デフォルトで主要なもの全て有効）
- `filter`プラグインは「フィルタ効果」と「カスタムオブジェクト」の両方の用途に使われる（今回は不採用だが将来的な他機能の参考になりうる）
- 開発・ビルドには`aviutl2-cli`（`au2`コマンド）を使用。`aviutl2.toml`にproject/artifactsを定義し、`au2 dev` / `au2 build`的なワークフローで配置する（symlink配置がデフォルト、Windows開発者モード要）
- 依存に`windows`クレート（DirectWriteバインディング含む）が既に含まれているため、エディタ側でプレビュー描画が必要になった場合はDirectWriteを直接叩ける

## 未着手・次のステップ

1. `module`側の`compositefont.resolve`関数のシグネチラ設計（引数: 文字またはコードポイント＋プロファイル参照、戻り値: フォント名・サイズ比・ベースラインシフト等の構造体）
2. 文字種／Unicode範囲の分類ルール（かな・カナ・漢字・欧文・数字・記号の境界定義）
3. プロファイルのファイルスキーマ（serde）設計
4. `generic`エディタのGUI設計・実装
5. PSDToolKit制御スクリプト側との結合テスト
